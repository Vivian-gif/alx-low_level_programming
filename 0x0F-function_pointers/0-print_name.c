#include "function_pointers.h"

/**
 * print: function pointer.
 *
 * Return: no return.
 */
v_name - prints a name.
 * @name: input name.
 * @foid print_name(char *name, void (*f)(char *))
{
	if (name && f)
		f(name);
}
